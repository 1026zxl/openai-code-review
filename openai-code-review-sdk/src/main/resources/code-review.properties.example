# OpenAI 代码评审 SDK 配置文件示例
# 复制此文件为 code-review.properties 并修改配置值

# ========== API配置 ==========
# API密钥（必需）
code.review.api.key=your_api_key_here

# API地址（可选，默认：https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions）
code.review.api.url=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions

# 模型名称（可选，默认：qwen-flash）
code.review.model=qwen-flash

# API密钥环境变量名（可选，默认：OPENAI_API_KEY）
code.review.api.key.env=OPENAI_API_KEY

# ========== 请求参数配置 ==========
# 温度参数（可选，默认：0.7，范围：0.0-2.0）
code.review.temperature=0.7

# 最大token数（可选，默认：4000）
code.review.max.tokens=4000

# ========== 报告存储配置 ==========
# 报告存储基础目录（可选，默认：代码评审记录）
code.review.report.base.dir=代码评审记录

# ========== GitHub 仓库配置 ==========
# GitHub 仓库 URL（可选，默认：https://github.com/1026zxl/code-review-repository.git）
code.review.github.repo.url=https://github.com/1026zxl/code-review-repository.git

# GitHub Token（可选，也可通过环境变量 GITHUB_TOKEN 设置）
# code.review.github.token=your_github_token_here

# GitHub Token 环境变量名（可选，默认：GITHUB_TOKEN）
code.review.github.token.env=GITHUB_TOKEN

# ========== Git配置 ==========
# Git仓库路径（可选，默认：当前目录）
# code.review.git.repository.path=/path/to/git/repo

# ========== 微信公众号配置 ==========
# 是否启用微信公众号推送（可选，默认：false）
# 如果配置了以下所有参数，将自动启用
code.review.wechat.enabled=false

# 微信公众号 AppID（测试号）
code.review.wechat.app.id=your_app_id

# 微信公众号 AppSecret（测试号）
code.review.wechat.app.secret=your_app_secret

# 模板消息ID（需要在微信公众平台配置模板）
code.review.wechat.template.id=your_template_id

# 接收消息的用户 OpenID（测试号中关注后获取）
code.review.wechat.open.id=your_open_id

# 环境变量名（可选）
code.review.wechat.app.id.env=WECHAT_APP_ID
code.review.wechat.app.secret.env=WECHAT_APP_SECRET

